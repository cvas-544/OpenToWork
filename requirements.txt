# OpenToWork — Python Dependencies
# Python 3.11+

# Claude API
anthropic>=0.40.0

# OpenAI (optional provider)
openai>=1.12.0

# Database
psycopg2-binary>=2.9.9

# HTTP requests (Arbeitsagentur)
requests>=2.31.0

# Apify (LinkedIn Jobs scraper)
apify-client>=1.7.0

# Environment variables
python-dotenv>=1.0.0

# Agent API server
fastapi>=0.110.0
uvicorn>=0.29.0

# Observability — Langfuse v2 (server v2 compatible; v4 SDK needs server v3)
langfuse==2.60.10

# Observability — AgentOps (hosted SaaS, compare vs Langfuse)
agentops>=0.3.0

# Async HTTP (traces proxy endpoints)
httpx>=0.27.0
